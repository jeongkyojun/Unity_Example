using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeySetUI : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Init();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void Init()
    {
        /*
        int yValue = 0;
        for(int i=0;i<10;i++)
        {
            //초기화
            item.Item(GetStudyData().result[0].level,
                GetStudyData().result[0].unit,
                GetStudyData().result[0].text_title);

            //Instantiate
            GameObject index = Instantiate(item, new Vector3(0, yValue, 0), Quaternion.identity);
            index.transform.SetParent(GameObject.Find("Content").transform);
            yValue -= 200;

        }
        */
    }
}
